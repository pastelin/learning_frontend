let arr = [9, 8, 14, 7, 6, 5, 4, 3, 2, 1];
const primo = (array) => {
  for(var i = 2; ; i++){
    if(array % i !== 0){
	
     }
  }

}
const val = arr.filter( primo(item) );

console.log(`Los números impares y ordenados son: ${val}`);
